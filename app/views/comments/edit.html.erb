<% puts @comment %>
<%= form_for @comment do |form| %>

    <%= form.label :comment, "Edit your comment:" %>
    <%= form.text_field :text, :value => @comment.text %></br></br>
    <%= form.hidden_field :log_id, :value => @comment.log_id %>

    <%= form.submit %>

<% end %>
